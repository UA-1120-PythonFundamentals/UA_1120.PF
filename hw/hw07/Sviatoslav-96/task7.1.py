def largest_number(number):
  """
  The function that returns the largest number from both numbers.
  
  Parameters:
  number - both numbers are provided in a list.

  Returns:
  The largest number from both numbers through max() function.
  """

  return max(number)

number = [10, 20]

print(largest_number(number)) 